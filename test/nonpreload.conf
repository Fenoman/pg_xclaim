## pg_xclaim non-preload smoke configuration
## Used by: make installcheck-nonpreload
## Intentionally NO shared_preload_libraries for pg_xclaim --
## verifies the XCLAIM_REQUIRE_INIT() path raises
## ERRCODE_OBJECT_NOT_IN_PREREQUISITE_STATE.

## No pg_xclaim entry here -- that's the point of this test
pg_xclaim.max_claims = 65536
pg_xclaim.num_partitions = 128
pg_xclaim.expected_claims_per_backend = 1024
