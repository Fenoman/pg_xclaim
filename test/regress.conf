## pg_xclaim main regression test configuration
## Used by: make installcheck (pg_regress main suite)
## Locale: ru_RU.UTF-8

shared_preload_libraries = 'pg_xclaim'
pg_xclaim.max_claims = 65536
pg_xclaim.num_partitions = 128
pg_xclaim.expected_claims_per_backend = 1024
pg_xclaim.enabled = on
